<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!--
      TODOS os mapeamentos <servlet> e <servlet-mapping> para os servlets
      (HomeServlet, SetorServlet, etc.) foram removidos.

      As anotações @WebServlet(...) dentro de cada arquivo .java já fazem
      esse trabalho de forma automática e moderna. Manter as duas
      configurações causa o erro.
    -->

    <!-- A configuração do Jersey para os Web Services é a única que precisa ficar aqui. -->
    <servlet>
        <servlet-name>JerseyRESTService</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>com.smartcity.webservice</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>JerseyRESTService</servlet-name>
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>

</web-app>